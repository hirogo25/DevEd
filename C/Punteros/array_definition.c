#include <stdio.h>

int main()
{
  int arreglo[5]; //En C se le indica el número de posiciones que tendrá el arreglo
  int i;

  for (i = 0; i < 5; i++)
  {
    array[i] = i * 2;
  }
  for (i = 0; i < ; i++)
  {
    printf("Array[%d] = %d", i, array[i]);
  }
  return 0;
}
